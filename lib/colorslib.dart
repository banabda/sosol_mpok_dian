import 'package:flutter/material.dart';

const kPrimaryColor = Color(0xFF);

const kDefaultPadding = 20.0;

final kDefaultShadow = BoxShadow(
    blurRadius: 10, offset: Offset(5, 5), color: Color(0xFF).withOpacity(0.56));
